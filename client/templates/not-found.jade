template(name="notFound")
  div(class="page not-found")
    +nav(title='Not Found')

    div.content-scrollable
      div.wrapper-message
        div.title-message Yikes! That wasn't found
        div.subtitle-message The stuff you were looking for wasn't found. 
          a(href="{{pathFor 'home'}}") Return home